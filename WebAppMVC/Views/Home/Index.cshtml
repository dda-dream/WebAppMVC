@model List<string>

<h1>HOME</h1>
@{

    <br />
    @Html.Raw( $"<a href='{Url.Content( "~/boomsosed.7z" )}'>Ссылка на boomsosed.7z</a>" )
    <br />
    Write( "test Write() " );
    Write( Html.Raw( "<br>" ) );
    foreach(var i in Model)
    {
         @Html.Raw(i) <br />
    }
}